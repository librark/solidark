# Solidark Showcase

This directory is a browser sample application for Solidark models. It demonstrates
primitive usage, CSG operations, feature intent, imported mesh placeholders, and
more complex model trees.

Run it locally with:

```sh
npm run showcase
```

Then open the printed local URL.
